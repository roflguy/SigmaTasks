namespace Helper
{
    public static class HelperClass
    {
        public static T[] UserCreateArray<T>()
        {
            int n = 0;
            WriteLine("Input quantity of elements: ");
            n = LegitIntInput();
            T[] Array = new T[n];

            return Array;
        }

        public static T[] CreateArray<T>(T length)
        {
            T[] Array = new T[n];

            return Array;
        }
    }
}
